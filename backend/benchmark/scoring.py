"""P3 owns — spec 05, task P3-M3.
score(task, answer) -> {"correct": bool, "score": float, "oracle": str}
Ground truth wins over judge where both exist; disagreement rate logged."""


def score(task: dict, answer: str) -> dict:
    raise NotImplementedError("P3-M3 — see specs/05-benchmark.md")
